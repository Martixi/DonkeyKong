//
// Created by admin on 20.12.2023.
//
#include "Other.h"

// Names of functions which were lacking in the sdl library shared on enauczanie
// They are not used, but my compilator did not work without them

void SDL_SetMainReady() {}
int SDL_ShowSimpleMessageBox(Uint32 flags, const char *title, const char *message, SDL_Window *window) { return 0; }
